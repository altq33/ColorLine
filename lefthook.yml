pre-commit:
  parallel: true
  commands:
    check-and-fix:
      glob: "*.{js,ts,jsx,tsx,json,css,scss,md}"
      run: bun x prettier --write {staged_files} && bun x eslint --fix {staged_files}
      stage_fixed: true

pre-push:
  commands:
    full-check:
      run: bun run check
